#include <bits/stdc++.h>
using namespace std;
#define ll long long int
#define nl '\n'
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    ll tCase = 1;
    cin >> tCase;
    while (tCase--)
    {
        ll n;
        cin >> n;
        cout << (n - 1) / 2 << '\n';
        // cout<<(n-3)/2+1<< nl;
    }
    return 0;
}