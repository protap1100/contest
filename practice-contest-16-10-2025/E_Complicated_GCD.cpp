#include <bits/stdc++.h>
using namespace std;
#define ll long long int;
#define nl '\n'
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    string a, b;
    cin >> a >> b;
    if (a == b)
    {
        cout << a << nl;
    }
    else
    {
        cout << 1 << nl;
    }

    return 0;
}